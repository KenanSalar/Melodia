// The worldwide directory. Empty until the phase that searches it — the page's chrome is the band
// above, so there is nothing here to stand in for.
export component RadioBrowseTab inherits Rectangle { }
